#pragma once

#include <iostream>
#include <algorithm>
#include <fstream>
#include <sstream>
#include <vector>
#include <string>

using namespace std;


#pragma once

enum program { programName, fileName, numcommandarg };
enum successAndFailure { success, fail , tooManyArgs};
